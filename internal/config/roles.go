package config

// Autarch is the identity label for the human principal of the sol system.
// From Greek autarchēs (self-ruler); cf. Gene Wolfe's Book of the New Sun.
// Used as the default actor in CLI commands, event emission, mail routing,
// and writ/caravan ownership.
const Autarch = "autarch"
