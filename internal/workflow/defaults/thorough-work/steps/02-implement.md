# Implement

Implement the changes for {{issue}} following your design.

1. Make the necessary code changes
2. Write or update tests as appropriate
3. Ensure the code compiles and tests pass
4. Commit your changes with clear messages

Stay within the scope of your design. If you discover the design needs
revision, note what changed and why in your commit message.

When implementation is complete and tests pass, advance:
`sol workflow advance --world=$SOL_WORLD --agent=$SOL_AGENT`
